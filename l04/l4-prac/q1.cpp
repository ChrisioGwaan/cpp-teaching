#include <iostream>

#include "card/Card.h"
#include "card/LinkedList.h"

#define RED_NUM 1
#define ORANGE_NUM 2
#define YELLOW_NUM 3
#define GREEN_NUM 4
#define BLUE_NUM 5
#define INDIGO_NUM 6
#define VIOLET_NUM 7

int main(void) {
    // LinkedList* cardList = new LinkedList();

    // Input from user, 
    // the input will be one of the `red`, `orange`, `yellow`, `green`, `blue`, `indigo`, `violet`
    // The input loop will end when the user input `exit` or `quit`
    // Implement an if-else statement, add a card to linkedlist with the color based on user input and its corresponding number
    // example for adding a card: cardList->add_front(new Card(RED, RED_NUM)); 



    return EXIT_SUCCESS;
}

// Implement the functions you need below